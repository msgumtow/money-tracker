/**
 * When I press button A, the money amount will change by 1. 
 * 
 * When I press another button, the money will go down by 1. 
 * 
 * When I shake the microbit, it will show the number "money" to display the total amount.
 */
input.onButtonPressed(Button.A, function () {
	
})
input.onButtonPressed(Button.B, function () {
	
})
input.onGesture(Gesture.Shake, function () {
	
})
